#include "zombie.h"
#include <iostream>
using namespace std;

        Zombie::Zombie()
        {
            color = color;                  // default constructor is implemented for the Zombie
        }
        void Zombie::setColor(char col)         // setter method for the Zombie's color is implemented
        {
            color = col;
        }

    


        char Zombie::getColor()
        {
            return color;                   // the getColor() returns the color of the Zombie simply, when called
        }

